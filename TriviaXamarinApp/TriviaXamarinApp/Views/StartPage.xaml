<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TriviaXamarinApp.Views.StartPage">
    <ContentPage.Content>
        
        <StackLayout>
            <Label Text="{Binding Question, StringFormat='Question: {0}'}"/>
            <CollectionView ItemsSource="{Binding answerList}" 
                SelectionMode="Single"
               SelectionChangedCommand="selectionComamd" SelectedItem="{Binding CorectAnswer}"  x:Name="answerslst">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="50">
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding}"/>
                                <Frame CornerRadius="1000" BackgroundColor="{Binding chooseanswer}"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <StackLayout Orientation="Horizontal">
                <Button  />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>